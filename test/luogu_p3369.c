#include <stdio.h>

int main() {

	int n=0;
	scanf("%d",&n);
	while(n--){
		int a,b;
		scanf("%d%d",&a,&b);
		switch(a){
		case 1: // insert(b)
		break;

		case 2: // delete(b,1)
		break;

		case 3: // rank(b), maynot exist
		break;

		case 4: // rank_k(b)
		break;

		case 5: // prev(b), maynot exist
		break;

		case 6: // next(b), maynot exist
		break;
		}
	}
	
	return 0;
}